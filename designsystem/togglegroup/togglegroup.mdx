import { Meta, Canvas } from '@storybook/blocks';
import * as stories from './togglegroup.stories';

<Meta of={stories} />

# ToggleGroup
- Bruk klassen `togglegroup` på typisk `<div>`
<Canvas of={stories.Default} />
