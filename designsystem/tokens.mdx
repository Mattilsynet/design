import { Meta } from '@storybook/blocks';
import { EyedropperIcon } from '@phosphor-icons/react';
import { JumpTo, Wide } from '../.storybook/blocks';
import { Colors, Radius, Sizes } from './tokens';
import { Divider } from './react';

<Meta title="Designsystem/Tokens" />
# <EyedropperIcon /> Tokens

> Design tokens styrer blant annet farger, størrelser og avstander på tvers av kode og Figma. 

<JumpTo />

## Farger
Vi benytter semantiske farge-tokens, for å knytte farger til meningsfulle roller i brukergrensesnittet, i stedet for spesifikke fargetoner.
Dette gir skalerbarhet, enklere vedlikehold, bedre tilgjengelighet og fleksibilitet.

- Bruk tokens *uten* spesifisert farge-palett - f.eks. `--mtds-color-BRUKSOMRÅDE-VARIANT`
- Sett heller fargepalett med attributten `data-color="NAVN"` - da arves paletten
- Bruk kun `text-*` og `base-contrast-*` på tekst/ikoner - da er kontrastkrav ivaretatt
- Unngå å blande farger fra ulike paletter for å ivarta kontrastkrav og dark mode
- Trenger du et mørkt område i light mode? Bruk `data-color="inverted"`

<br />
<Wide><Colors /></Wide>

<Divider data-gap={15} />

## Dark mode

Fargemodus kan settes med `data-color-scheme="auto | light | dark"` på `<html>`, hvor `auto` er default og følger brukerens systeminnstillinger.

<Divider data-gap={15} />

## Størrelser og spacing
Size tar utgangspunktet i til en hver tid gjeldene `font-size`, som igjen påvirkes av skjermstørrelse.
Det betyr at du ikke alltid kjenner den eksakte pikselverdien, men i stedet kan fokusere på forholdet mellom elementene.
Du kan bruke skalane i [alle Tailwind klasser](https://tailwindcss.com/docs/) selv om kun `padding` og `margin` er vist i tabellen under.
<Sizes />

<Divider data-gap={15} />

## Radius
<Radius />

<Divider data-gap={15} />

## Tailwind
Dersom du ønsker å bruke [TailwindCSS](https://tailwindcss.com/) sammen med Mattilsynet Designsystem, kan du både installere `@mattilsynet/design` og [installere Tailwind](https://tailwindcss.com/docs/installation/), og importere stiler som følger:

```css
/* Load @mattilsynet/design before Tailwind: */
@import '@mattilsynet/design/styles.css';

/* Get Mattilsynet tokens as Tailwind config: */
@import '@mattilsynet/design/tailwind.css';
@import 'tailwindcss';
```
Da får du automatiske Tailwind-verdier for `--color`, `--spacing`, `--border-radius` og `--font-weight` i Tailwind v4 oppsett.

