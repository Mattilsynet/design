import { Meta, Canvas } from '@storybook/blocks';
import * as stories from './chip.stories';

<Meta of={stories} />

# Chip (Eksperimentell)

- Bruk klassen `chip` på typisk `<span>`

<Canvas of={stories.Default} />