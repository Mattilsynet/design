import { defineComponentContract } from "../component-contract";

export default defineComponentContract({
  "name": "Ingress",
  "className": "ingress",
  "tag": "span",
  "content": true
} as const);
