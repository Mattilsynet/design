import { Meta, Canvas } from '@storybook/blocks';
import { Alert } from '../react.tsx';
import * as stories from './layout.stories';

<Meta of={stories} />

# Layout
- Bruk klassene `flex` for `display: flex` eller `grid` for `display: grid`
- Begge klassene støtter `data-align`, `data-justify`, `data-align-content`, `data-center`, `data-gap`, `data-items`, `data-fixed`, `data-nowrap`
- Begge klassene støtter `data-items="50 | 100 | 150 | 200 | 250 | 300 | 350 | 400 | 450 | 500"` for minimumsbredder som får lov å vokse


<Alert data-color='info'>
**Hvorfor ikke bare bruke Tailwind eller vanilla CSS?**
<br />Fordi vi ønsker å ha en felles standard for layout som er enkel å forstå og bruke. Dette gjør det enklere å lage nye komponenter og sider, og gjør det enklere å vedlikeholde eksisterende kode.
</Alert>

## Flex
- **💡 Bruk `flex` når du trenger forskjellige bredder på elementene**
- `flex-wrap: wrap` er på default, men kan skrues av med `data-nowrap`
- **Merk:** Skal du kun elementer under hverandre, er `grid` et bedre alternativ, siden `flex-direction: column` kan ha uønskede bieffekter på f.eks. `align-items`, `justify-content`.

<Canvas of={stories.FlexStory} />

## Grid
- **💡 Bruk `grid` når du trenger samme bredder på alle elementer**
- **💡 Bruk `grid` *uten* `data-items` for å sette elementer under hverandre**
- Bruk `data-items="auto"` for automatisk antall kolonner basert på antall elementer inni
- Bruk `data-items="50 | 100 | 150 | 200 | 250 | 300 | 350 | 400 | 450 | 500"` for minimumsbredder som får lov å vokse
- Bruk `data-fixed` for å låste bredder
<Canvas of={stories.GridStory} />

## Gap
- `data-gap="0 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8"` (`3` er default)
<Canvas of={stories.Gap} />

## Center
- Bruk `data-center="sm | md | lg | xl | 2xl"` for å sentrere innholdet
- Elementet vil allid ha minimum `5vw` luft på venste og høyre side
- Vi bruker hovedsakelig `2xl` som hoved container for innhold
- Virker på både `flex` og `grid`
<Canvas of={stories.Center} />

## Align
- `data-align="normal | start | center | end | stretch"` (`normal` er default) setter vertikal plassering med [align-items](https://developer.mozilla.org/en-US/docs/Web/CSS/align-items) 
- Virker på både `flex` og `grid`
<Canvas of={stories.Align} />

## Justify
- `data-justify="start | center | end | space-between | space-around | space-evenly"` setter horisontal plassering med [justify-content](https://developer.mozilla.org/en-US/docs/Web/CSS/justify-content) 
- Virker på både `flex` og `grid`
<Canvas of={stories.Justify} />

## AlignContent
- `data-align-content="start | center | end | space-between | space-around | space-evenly"` setter vertikal plassering over flere rader med [align-content](https://developer.mozilla.org/en-US/docs/Web/CSS/align-content)
- Virker på både `flex` og `grid`
<Canvas of={stories.AlignContent} />

## App (Experimental)
- Bruk klassen `app` og legg `<header>`, `<nav>`, `<main>`, `<aside>` og `<footer>` som barn
- Alle elemetene er valgfrie, og kan grupperes i `<div>`-er dersom du trenger definerte områder
<Canvas of={stories.App} />