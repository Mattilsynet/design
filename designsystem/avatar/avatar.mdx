import { Meta, Canvas } from '@storybook/blocks';
import * as stories from './avatar.stories';

<Meta of={stories} />

# Avatar (Eksperimentell)

- Bruk klassen `avatar` på typisk `<span>`

<Canvas of={stories.Default} />